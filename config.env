# VectreC toolchain (CMOC + lwtools + stdlib)
VECTREC="${VECTREC:-$HOME/retro-tools/vectrec-macos-arm64}"

# Vecx emulator (.app bundle)
VECX_APP="${VECX_APP:-/Users/brianjones/Downloads/Vecx v0.2.0/Vecx.app}"

# Output ROM path (relative to project root)
ROM="build/sphere-link.bin"
